id: panel-latch-switch
kind: other
source_claim: >-
  That the latching switch bought for the panel is compatible with J3, whose
  lamp leg feeds +5V through R5 with R5 fitted as a 0 ohm link and therefore
  provides NO current limiting of its own.
source_ref: >-
  docs/power-sheet.md "Latch switch J3" and "R5 is a 0 Ohm link because the
  3-9 V lamp limits its own current"; docs/indicators.md "1. Switch lamp";
  Matt 2026-09-05, AliExpress 3256802323373047, "12mm white light 3-6V is what
  is stated on the side of the switch".
resource: >-
  The 12 mm illuminated latching pushbutton for the panel, against caryatid J3
status: confirmed      # 2026-09-20. Compatible with J3, and the lamp current is now MEASURED.

measured_2026_09_06:
  # Matt, with the switch in hand and the bench supply. Everything that was
  # open on 2026-09-05 is now closed except the lamp CURRENT.
  action: LATCHING. Confirmed by hand.
  pins: 4
  layout: >-
    Two pins LABELLED + and -, which are the ring LED. Two UNLABELLED, which
    are the switch contacts. The simplest and best of the possible
    arrangements: a plain SPST, no NC contact to choose wrongly.
  switch_contacts:
    latched_in_ohm: 0.9
    unlatched: open
    therefore: >-
      NORMALLY OPEN, closing when the button is latched in. THAT IS EXACTLY THE
      SENSE J3 WANTS: the boost is off until the button is pressed in, so off
      means genuinely off. Had it been the other way round the instrument would
      have powered up with the button OUT.
  lamp: >-
    Run directly from the 5 V bench supply with no damage. That corroborates
    the 3-6V marking empirically as well as by inference.
    CURRENT MEASURED 2026-09-20, see `measured_2026_09_20` below.
  listing_specs_as_supplied_by_matt:
    rating_switch: "3A / 250VAC"
    ingress: IP65
    body: oxidised aluminium
    emitting_type: "Ring LED"
    opening_diameter: "12 mm (the variant bought)"
    lamp_voltage_options: "3/6/12/24/36/110/220 V, this unit marked 3-6V/W"

  🔴 the_3A_rating_is_irrelevant_and_worth_understanding_why: >-
    THIS SWITCH DOES NOT CARRY THE INSTRUMENT'S POWER. It asserts the boost's
    enable and nothing else. J3.1 is VOUT and J3.2 is EN_SW, which sits on
    R6, a 100 kohm pulldown, so the current through the closed contacts is

        VOUT / 100k  =  about 42 uA at a 4.2 V cell

    MICROAMPS, NOT AMPS. The boost does the actual power switching, on the
    board, through L1. So the 3 A / 250 VAC rating is enormous overkill, the
    0.9 ohm contact resistance is irrelevant, and any small switch would do.

    Worth stating because the opposite assumption is the natural one, and it is
    what makes people specify a heavy switch, run fat wire to the panel, and
    worry about contact resistance that cannot matter. Contrast the HOOK
    switch, where 0.9 ohm against a 10 kohm pull-up WOULD have mattered: see
    `loa-hook-switch`, threshold 2.69 kohm.

the_wiring_is_now_settled:
  J3.1: "VOUT       -> either unlabelled pin. A plain SPST has no polarity"
  J3.2: "EN_SW      -> the other unlabelled pin"
  J3.3: "lamp anode -> the pin labelled +"
  J3.4: "lamp cathode -> the pin labelled -"
  note: >-
    Build this as the four-way cable per the colour convention: red, yellow,
    orange, black. Still make the BARE pins 1-2 link separately for bring-up
    Stages 2 and 3, which keeps the lamp out of the boost test.

measured_2026_09_20_THE_LAMP_CURRENT_IS_CLOSED:
  # Matt, at the bench. 10.2 ohm shunt, Fluke 101 on DC mV, Jesverty supply
  # in CC with a 100 mA limit. NO BOARD. Evidence file:
  # discovery/evidence/2026-09-20-switch-lamp-current.txt
  headline: "28.5 mA at 5.00 V across the lamp. 5.6 mA at 3.00 V."

  readings:
    - {supply: "3.00 V", shunt_mV: 51.5, current_mA: 5.05,  lamp_V: 2.949}
    - {supply: "5.00 V", shunt_mV: 257.9, current_mA: 25.28, lamp_V: 4.742}
    - {supply: "5.29 V", shunt_mV: 290.5, current_mA: 28.48, lamp_V: 5.000, note: "IN SERVICE"}

  🔴 why_there_are_three_readings_and_not_two: >-
    THE SHUNT DROP IS NOT NEGLIGIBLE AT THIS CURRENT, and the first pass
    assumed it would be. At 25 mA a 10.2 ohm shunt drops 258 mV, so the
    reading taken at a 5.00 V SETPOINT had only 4.74 V on the lamp. That is
    an 11% error on the quantity wanted, not the 3% that was predicted from
    an assumed 15 mA.

    The third reading sets the supply to 5.29 V so that the drop leaves
    exactly 5.00 V on the lamp. THAT is the in-service figure, because R5 is
    a 0 ohm link and the lamp sees the full rail with nothing in the way.

    GENERAL LESSON FOR THIS RUNBOOK: with a series shunt you are measuring at
    a load voltage you did not set. Either add the drop back into the
    setpoint, or record the voltage the load actually saw. Do not quote the
    setpoint.

  what_the_three_points_show: >-
    THEY FIT A STRAIGHT LINE. The "internal limiting" that a voltage-range
    marking implies is A PLAIN SERIES RESISTOR, not a constant-current driver:

        Vf ~ 2.51 V,  R_series ~ 87.5 ohm

    Model against measurement: 5.06 vs 5.05, 25.54 vs 25.28, 28.19 vs 28.48.
    Within 1% across the span, and the residual is the LED's own Vf rising
    with current, which is what a real diode does.

  🔴 consequence_the_current_is_NOT_held_across_the_range: >-
    A voltage RANGE marking means the part tolerates the range. It does NOT
    mean the current is constant over it. Through a series resistor the
    current tracks the rail almost 8:1 end to end:

        3.00 V  ->   5.6 mA
        5.00 V  ->  28.5 mA   (measured)
        6.00 V  ->  39.9 mA   (extrapolated, top of the marking)

    SO THE BUDGET LINE IS ONLY MEANINGFUL QUOTED AGAINST A RAIL VOLTAGE.
    This does not weaken the 2026-09-05 compatibility pass, which turned on
    the part tolerating 5 V, and it does. It qualifies what the phrase
    "internally limited" was taken to promise.

  provenance_caveat: >-
    The 10.2 ohm shunt was measured 2-WIRE and the LEAD RESISTANCE WAS NOT
    SUBTRACTED. Clip leads are typically 0.1-0.3 ohm, so the true shunt may
    be nearer 10.0 ohm and every current above may read up to ~2% LOW: 28.5
    could be as much as 29.1. NOT CORRECTED, because it changes no
    conclusion. Subtract the leads if this is ever needed better than 2%.

  it_confirms_a_prediction_in_indicators_md: >-
    indicators.md said, conditionally, "if the limiting is a plain series
    resistor, roughly 75% of the current, which the eye reads as around 85%
    as bright", comparing 5 V against a 6 V 4xAA reference. IT IS a plain
    series resistor, and the real ratio is 28.5/39.9 = 71%. The estimate was
    right and slightly optimistic, and the brightness conclusion stands.

  where_it_lands_in_the_budget: >-
    28.5 mA makes the lamp THE LARGEST INDICATOR LOAD on the 5 V rail, ahead
    of the RGB with all three dies lit at 18.9 mA, and unlike the RGB it is
    lit the whole time the instrument is on.

    IT DOES NOT ADD TO THE RUNTIME TABLE. The 150/250/350 mA scenario rows
    are top-down estimates, not a tally, and values.md says so. The tally
    with the lamp in it reaches ~141 mA in electret mode and ~163-183 mA in
    carbon, against a 250 mA typical row, so 28.5 mA lands INSIDE the
    existing headroom rather than on top of it. Runtime figures unchanged.

    Two of the three unattributed lines remain: the mic bias pair, and the
    three connector 5 V pins.

the_lamp_question_is_ANSWERED:
  reading: "3-6V, marked on the body of the switch"
  verdict: 🔴 PASS. Wire it to J3.3 / J3.4 with R5 left as the 0 ohm link.
  why: >-
    A LAMP RATED AS A VOLTAGE RANGE IS INTERNALLY LIMITED. That is what the
    range means: the part contains its own resistor or driver and takes any
    voltage across the span. A bare LED cannot be given a range, it is given a
    forward voltage and a current.

    5.0 V sits inside 3-6 V, so the +5V rail through a 0 ohm R5 is exactly the
    intended arrangement and nothing needs changing.

  variance_from_the_documents_and_why_it_does_not_matter: >-
    indicators.md and power-sheet.md both describe a 3-9 V variant.
    ✅ BOTH CORRECTED 2026-09-20, along with values.md, sourcing.md and
    capture-checklist.md, when the current measurement landed. THIS SWITCH
    IS 3-6 V. That is a different part, and it is FINE, and it is slightly
    BETTER at this rail rather than worse.

    indicators.md worried that 5.0 V would run the lamp below the ~6 V it was
    judged against, "roughly 75% of the current, which the eye reads as around
    85% as bright". Against a 6 V top rather than a 9 V one, 5 V is 83% of the
    range rather than 56%. The brightness concern shrinks. NO ACTION, and the
    documents are not wrong, they describe a variant that was not bought.

superseded_still_unknown_1_LATCHING_OR_MOMENTARY:
  why_it_matters: >-
    THE BOARD REQUIRES LATCHING. R6 holds the boost's EN low, and the switch
    pulls EN high from VOUT through J3.1 to J3.2. That is what makes it a real
    power switch: press once and the instrument stays on.

    A MOMENTARY SWITCH POWERS THE BOARD ONLY WHILE HELD DOWN. It would work
    perfectly on the bench for as long as a finger is on it, which is exactly
    long enough to pass every test and fail as an instrument.
  the_test: >-
    PRESS IT. Latching clicks and STAYS IN, and clicks out on a second press.
    Momentary springs back. No meter needed and it takes a second.
    Listings call latching "self-locking", "maintained" or "on/off"; momentary
    is "reset" or "self-reset". The listing was not read: AliExpress redirected
    to a login endpoint and was not followed.

superseded_still_unknown_2_THE_PINOUT:
  why_it_matters: >-
    A 12 mm illuminated pushbutton is commonly 4 pins (2 switch, 2 lamp), 5 pins
    (SPDT: common, NO, NC, plus 2 lamp) or 6. Which is which is not guessable,
    and J3 wants exactly two of the switch pins and exactly two lamp pins.
  the_test: >-
    Beep it out, the same way the hook switch is being beeped out, and the same
    lesson applies: NAME THE TERMINALS, NOT THE SWITCH.
  the_discriminator: >-
    THE SWITCH CONTACTS READ ABOUT 0 OHM IN ONE STATE. THE LAMP PAIR NEVER
    DOES. An internally limited lamp has a resistor or driver in series, so it
    will not show a clean diode drop and will never short. That separates the
    two functions in one sweep without any assumption about pin order.
  which_switch_pins_j3_wants: >-
    If it is SPDT, take COMMON and NORMALLY OPEN, so that the boost is off
    until the button is latched in. NC would power the instrument when the
    button is OUT, which is backwards.
  lamp_polarity: >-
    An LED lamp is polarised. J3.3 is the ANODE side (+5V through R5) and J3.4
    the CATHODE (GND). Backwards it simply will not light; with an internal
    series resistor there is no damage at 5 V. Identify it by applying the
    bench supply across the lamp pair and seeing which way round it lights.

superseded_the_measurement_that_closes_a_DOCUMENTED_unknown:
  # ✅ DONE 2026-09-20. Kept because it states WHY the reading was worth
  # taking, and the answer is in `measured_2026_09_20...` above.
  # It needed NO BOARD. Supply, meter, switch. It was done in ten minutes.
  what: >-
    Meter in SERIES with the lamp, on the 5 V bench supply. Record the current.
  why_it_is_worth_doing_now: >-
    values.md lists three unattributed items in the 5 V power budget, and one of
    them is "THE SWITCH LAMP IS UNMEASURED". capture-checklist.md has been
    asking for a meter in series at both voltages since capture. This reading
    lands directly in that table and closes one of the three.

    It also feeds the runtime table, since the lamp is lit the entire time the
    instrument is on. At 350 mA "loud, LEDs lit" the runtime is 5.2 hours, and
    the lamp is currently a guess inside that number.
  also_record: >-
    The current at 3 V as well if convenient, since capture-checklist asks for
    both ends. It brackets what the lamp does as the rail moves.

mounting:
  stated: "12 mm"
  🔴 what_that_figure_IS: >-
    12 mm is the MOUNTING HOLE diameter, which is how this class of switch is
    designated. IT IS NOT THE BEZEL DIAMETER, which is larger, typically
    14-15 mm on a 12 mm switch.
  consequence_for_loa: >-
    loa docs/design/12-phone-build.md carries "the latching switch (bezel
    diameter still to measure)" under panel positions still to take. THIS
    PARTIALLY ANSWERS IT: the hole is 12 mm, which is what gets drilled. The
    bezel diameter, which is what decides whether it clears its neighbours on
    the phone's panel, still wants calipers across the flange.

evidence:
  - "2026-09-20: LAMP CURRENT MEASURED, closing bringup A7.6 and one of the
     three unattributed lines in the 5 V budget. 28.5 mA at 5.00 V across the
     lamp, 5.6 mA at 3.00 V. 10.2 ohm shunt, Fluke 101 on DC mV, Jesverty
     supply in CC at a 100 mA limit, no board. Three points fit Vf 2.51 V with
     an 87.5 ohm series resistor within 1%, so the internal limiting is a
     PLAIN RESISTOR and the current is not held across the 3-6 V marking.
     The shunt drop is not negligible at this current: the reading taken at a
     5.00 V setpoint had only 4.74 V on the lamp, hence a third reading at a
     5.29 V setpoint. Shunt measured 2-wire with leads not subtracted, so the
     figures may read up to 2% low
     (discovery/evidence/2026-09-20-switch-lamp-current.txt)"
  - "2026-09-06: SWITCH CONFIRMED COMPATIBLE WITH J3. Latching, 4 pins, two
     labelled +/- for the ring LED and two unlabelled for the contacts. The
     contacts read 0.9 ohm latched in and open when out, so NORMALLY OPEN,
     which is the sense J3 needs: the boost stays off until the button is
     pressed in. Lamp run directly from the 5 V bench supply without damage.
     Listing gives 3A/250VAC, IP65, oxidised aluminium, ring LED. That current
     rating is irrelevant here: the switch carries only the boost's enable
     through R6's 100 kohm pulldown, about 42 uA, not the instrument's power"
  - "2026-09-05: lamp rated 3-6V, read by Matt off the body of the switch. A
     voltage RANGE means internal current limiting, so J3's 0 ohm R5 is correct
     and 5.0 V sits inside the range. This CLOSES the lamp compatibility
     question. The documents describe a 3-9 V variant; this is a different part
     and is slightly better at 5 V, since 5 V is 83% of a 6 V top against 56%
     of a 9 V one"
  - "2026-09-05: the listing was NOT read. AliExpress 3256802323373047
     redirected to a login endpoint, which was not followed. Switch action
     (latching vs momentary), pin count and pinout are therefore unestablished
     and are not inferred from the product category"

last_verified: 2026-09-20

notes: >-
  CABLE CHECK 1 IS CLOSED and the J3 cable can be built. The other two are
  untouched and both concern the LEDs: common anode on J4 and J12, and whether
  the charge LED's green die is AlGaInP or InGaN.

  IT NEVER BLOCKED THE BENCH. J3 needs only a BARE LINK between pins 1 and 2
  for bring-up Stages 2 and 3, and that link deliberately leaves the lamp out
  of the boost test.

  WHAT THIS RECORD ACTUALLY CAUGHT is worth keeping. The lamp question was real:
  R5 is a 0 ohm link and a bare LED there dies at 5 V. It resolved to a pass, and
  it resolved because the answer was READ OFF THE PART rather than assumed from
  the product category. The listing itself was never fetched, and the listing
  would not have said which of seven lamp voltages this unit carries.
